/**
 * Where the contact form goes.
 *
 * Placeholder address until the real one is supplied; it is what the page shows
 * and what the mail-client fallback opens.
 */
export const CONTACT_EMAIL = 'loremipsum@gmail.com';

/**
 * A form endpoint that forwards straight to CONTACT_EMAIL — Formspree, Basin,
 * Netlify Forms, a worker of your own, anything that accepts a JSON POST.
 * Leave it empty and the form composes the message into the visitor's own mail
 * client instead, so the page is never a dead end while this is unset.
 */
export const CONTACT_ENDPOINT = '';
